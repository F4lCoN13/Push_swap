#include "../include/headerpush_swap.h"

int   main(int args, char **argv)
{
     (void)args;
     if (ft_find_error(argv) == 0)
          return (0);
     ft_printf("\nc'est bon ca marche !!! \n");

}

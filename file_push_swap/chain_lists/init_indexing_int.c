#include "../../include/headerpush_swap.h"

void  ft_indexing(t_tab *tab, t_chain **head)
{
   int   i;
   int   tmp;
   t_chain *p;

   i = 0;
   while (i < tab->size)
   {
      p = *head;
      while (p != NULL)
      {
         if (
      }
   }
   ft_printf("tab->size = %d coucou je suis la head =%d", tab->size, (*head)->nb);
}
